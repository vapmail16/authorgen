app = "author-site"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"
  args = { SITE = "arjun-singh-2025-10-16" }

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[services]]
  protocol = "tcp"
  internal_port = 80

